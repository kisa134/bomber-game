/**
 * Deferred — hub liquid-glass is NOT the landing CTA style.
 * Landing uses pixel-bevel `.cta-yellow` / `.cta-ghost` (see globals.css).
 * Keep this module for future pixel-glass HUD chips only, not primary CTAs.
 */
"use client";

export function LiquidGlassButton() {
  return null;
}
